<h1>Booking Details</h1>
<p><strong>Flat:</strong> <%= @booking.flat.name %></p>
<p><strong>Check-in:</strong> <%= @booking.start_datetime.strftime("%B %d, %Y") %></p>
<p><strong>Check-out:</strong> <%= @booking.end_datetime.strftime("%B %d, %Y") %></p>
<p><strong>Guests:</strong> <%= @booking.guests %></p>

<% nights = booking_nights(@booking) %>
<p><strong>Total Price:</strong>
  <%= "#{number_to_currency(@booking.flat.price_per_night * nights * @booking.guests, unit: '', delimiter: '.', precision: 0)} gold" %>
</p>
